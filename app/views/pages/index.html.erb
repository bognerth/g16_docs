<h3>Dokumentenliste</h3>

<table>
  <tr>
    <th>Titel</th>
    <th>Beschreibung</th>
    <th>Kategorie</th>
    <th>Kurs</th>
    <th>Dwonload</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.title %></td>
    <td><%= page.description %></td>
    <td><%= page.category %></td>
    <td><%= page.lecture %></td>
    <td><%= link_to 'D', download_page_path(page) %></td>
    <td><%= link_to 'Show', page %></td>
    <td><%= link_to 'Edit', edit_page_path(page) %></td>
    <td><%= link_to 'Destroy', page, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Page', new_page_path %>
