
<p>
  <b>Titel:</b>
  <%= @page.title %>
</p>

<p>
  <b>Beschreibung:</b>
  <%= @page.description %>
</p>

<p>
  <b>Inhalt</b>
  <%= @page.to_yaml %>
  <pre>
    <%= File.read Rails.root.join("public/#{@page.file_url}") if @page.content_type == 'text/plain' %> 
  </pre>
</p>
<%= link_to 'Back', request.referer %>
